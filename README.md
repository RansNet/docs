# docs

installation guide

- install vscode
- create a local folder, use vscode to pull docs repo from github
- install mkdoc and mkdoc-material
    - install python https://www.python.org/
    - python get-pip.py
    - pip install mkdocs
    - pip install mkdocs-material
    - pip install mkdocs-glightbox